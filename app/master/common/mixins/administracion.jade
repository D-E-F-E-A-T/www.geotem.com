mixin item(img, name, office)
	.item
		img(src='/static/img/acerca/administracion/#{img}.png')
		h3 #{name}
		p #{office}


mixin overlay(image, metodo,metodo1,fuente,number, title)
	.caja
		.imagen
			img(src="/static/img/metodos/#{image}.png")
		.description

			span
				|Método : #{metodo}
				br
				|#{metodo1}				
				br
				br
			span
				|Fuente: #{fuente}
				br
				br
			span
				|Profundidad de
				br
				|Investigación: #{number}

	.cintillo
		p #{title}


mixin overlay2(image, metodo,metodo1,res,ref, title)
	.caja
		.imagen
			img(src="/static/img/metodos/#{image}.png")
		.description
			span 	
				|Método : #{metodo}
				br
				|#{metodo1}
				br
				br
			span
				|Resolución: #{res}
				br
				br

			span #{ref}

	.cintillo
		p #{title}

mixin overlay3(image, metodo,fuente,ref, title)
	.caja
		.imagen
			img(src="/static/img/metodos/#{image}.png")
		.description
			p 	
				|Método : #{metodo}
				br
				br
			p
				|Fuente: #{fuente}
				br
				br
			p #{ref}

	.cintillo
		p #{title}

mixin overlay4(image, title,description)

	.caja
		.imagen
			img(src="/static/img/metodos/#{image}.png")
		.description
			p 	
				|Método : #{metodo}
				br
				br
			p
				|Fuente: #{fuente}
				br
				br
	.cintillo
		p #{title}






